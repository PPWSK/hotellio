<%= simple_form_for([@accommodation, @picture], html: { multipart: true }) do |f| %>
  <%= f.error_notification %>

  <%= f.input :caption, required: false %>
  <p>
    <%= f.label :type %>
    <%= f.file_field :type %>
  </p>

  <%= f.button :submit %>
<% end %>
